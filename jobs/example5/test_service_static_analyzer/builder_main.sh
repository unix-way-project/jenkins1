#!/bin/bash

set -e

main() {
  echo "Starting to run Static analizer tool on the code in the repository for service ${SERVICE}"
  echo "Job will pass only if project mets all criteria"

  sleep 10
}

main
