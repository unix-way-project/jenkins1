#!/bin/bash

set -e

main() {
  echo "Initializing all clients for backup database for service ${SERVICE}"
  echo "Here we create all necessary configuration files for backup to happen"
  echo "For example, it can be initialization of AWS config file for accessing RDS"
}

main
